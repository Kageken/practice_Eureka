# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'practice_Eureka' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Eureka'

  # Pods for practice_Eureka

  target 'practice_EurekaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'practice_EurekaUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
